spring:
  application:
    name: Backend

  # --- DB 설정 ---
  datasource:
    url: jdbc:mysql://dev-ttokttok.cjyuygmyir9b.ap-northeast-2.rds.amazonaws.com:3306/ttokttok
    username: root
    password: ${DB_PASSWORD} # ✅ 환경 변수 필요
    driver-class-name: com.mysql.cj.jdbc.Driver

  # --- JPA/Hibernate 설정 ---
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        show_sql: true
        format_sql: true
        use_sql_comments: true

  # --- 메일 설정 ---
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${SMTP_EMAIL} # ✅ 환경 변수 필요
    password: ${SMTP_PASSWORD}     # ${SMTP_PASSWORD}  # ✅ 환경 변수 필요
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

cloud:
  aws:
    s3:
      bucket: ttokttok-bucket
      path:
        notice: notices
        voice-memo: voice-memos
    region:
      static: ap-northeast-2
    stack:
      auto: false
    credentials:
      accessKey: ${AWS_ACCESS_KEY_ID}
      secretKey: ${AWS_SECRET_ACCESS_KEY}

  # --- JWT 설정 ---
  jwt:
    secret: ${JWT_KEY}
    access-token-validity-in-seconds: 3600
    refresh-token-validity-in-seconds: 86400


  logging:
    level:
      org.springframework.security: DEBUG